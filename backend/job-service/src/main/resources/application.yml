server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: job-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
